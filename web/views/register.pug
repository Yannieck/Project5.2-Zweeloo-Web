html
    head
        include head.pug
        title Aanmelden

    body.h-full.clr-bg-main(class="border-[20px] clr-border-sec")
        include header.pug
        block content
            .w-full.flex.flex-col.py-12.px-8.overflow-y-auto(class="h-[calc(100%-4rem)] md:px-32")
                .px-16.w-full                
                    script.
                        let message = "#{status}";
                    script(src="/scripts/swal.js")

                    h1.text-4xl.font-bold.mb-5 Profiel aanmaken

                    form(method="post" action="/auth/register")
                        .flex.w-full.gap-4
                            div(class="w-1/2")
                                label.font-bold.block(for="first_name") Voornaam
                                input.py-2.px-4.mb-4.w-full(type="text" name="first_name" placeholder="Voornaam..." required)
                            div(class="w-1/2")
                                label.font-bold.block(for="last_name") Achternaam
                                input.py-2.px-4.mb-4.w-full(type="text" name="last_name" placeholder="Achternaam..." required)

                        label.font-bold.block(for="email") Email
                        input.py-2.px-4.mb-4.w-full(type="email" name="email" placeholder="Email..." required)

                        label.font-bold.block(for="password") Wachtwoord
                        input.py-2.px-4.mb-4.w-full(type="password" name="password" placeholder="Wachtwoord..." required)

                        label.font-bold.block(for="password_repeat") Herhaal wachtwoord
                        input.py-2.px-4.mb-4.w-full(type="password" name="password_repeat" placeholder="Herhaal wachtwoord..." required)

                        button.btn.mt-4(type="submit") Creëer account
                .line